
module.exports = (commerceModels) => {
  const debug = require('debug')('component:commerce:transactionstatus:model');
  const transactionStatusModel = commerceModels.TransactionStatus;

  let transactionStatus = {};
  return transactionStatus;
};
