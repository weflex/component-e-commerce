
module.exports = (commerceModels) => {
  const debug = require('debug')('component:commerce:discounttype:model');
  const discountTypeModel = commerceModels.DiscountType;

  let discountType = {};
  return discountType;
};
